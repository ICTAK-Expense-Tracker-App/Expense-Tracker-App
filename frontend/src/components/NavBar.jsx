import { AppBar, Button, Toolbar } from '@mui/material'
import React from 'react'

const NavBar = () => {
  return (
    <div>
        <AppBar>
            <Toolbar>
                <Button></Button>
            </Toolbar>
        </AppBar>
    </div>
  )
}

export default NavBar